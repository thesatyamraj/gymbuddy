# ============================================================
# WEB — PRODUCTION ENVIRONMENT (set these in Vercel dashboard)
# Point these at your deployed Render backend URL (no trailing slash).
# ============================================================

# Render backend URL + /api
VITE_API_URL=https://your-api.onrender.com/api

# Render backend URL (root, for Socket.io)
VITE_SOCKET_URL=https://your-api.onrender.com
